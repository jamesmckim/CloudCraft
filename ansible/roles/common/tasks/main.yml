# 2. Pre-flight Checks
- name: Assert required variables are defined
  ansible.builtin.assert:
    that:
      - internal_ip is defined
      - age_version is defined
      - sops_version is defined
      - helm_version is defined
    fail_msg: "A required variable is missing. Check your variable definitions."
  tags: [always]

# 3. Base Utilities
- name: Ensure apt cache is updated
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: 3600
  tags: [base, packages]

- name: Install base system utilities
  ansible.builtin.apt:
    name:
      - curl
      - wget
      - apt-transport-https
      - jq
      - tar
      - iptables-persistent
    state: present
  tags: [base, packages]

# 4. Tool Installations
- name: Install Age (Encryption Tool)
  ansible.builtin.unarchive:
    src: "https://github.com/FiloSottile/age/releases/download/{{ age_version }}/age-{{ age_version }}-linux-amd64.tar.gz"
    dest: /usr/local/bin/
    remote_src: yes
    extra_opts:
      - --strip-components=1
      - age/age
      - age/age-keygen
    creates: /usr/local/bin/age
  tags: [tools, security]

- name: Install SOPS (Secrets OPerationS)
  ansible.builtin.get_url:
    url: "https://github.com/getsops/sops/releases/download/{{ sops_version }}/sops-{{ sops_version }}.linux.amd64"
    dest: /usr/local/bin/sops
    mode: '0755'
    # checksum: "{{ sops_checksum }}" # Uncomment when checksums are available
  tags: [tools, security]

- name: Install Skaffold
  ansible.builtin.get_url:
    url: "https://storage.googleapis.com/skaffold/releases/latest/skaffold-linux-amd64"
    dest: /usr/local/bin/skaffold
    mode: '0755'
  tags: [tools, k8s]

- name: Install Helm
  ansible.builtin.unarchive:
    src: "https://get.helm.sh/helm-{{ helm_version }}-linux-amd64.tar.gz"
    dest: /usr/local/bin/
    remote_src: yes
    extra_opts:
      - --strip-components=1
      - linux-amd64/helm
    creates: /usr/local/bin/helm
  tags: [tools, k8s]

# 8. Post-Execution Verification
- name: Verify tool installations
  ansible.builtin.command: "{{ item.name }} {{ item.arg }}"
  loop:
    - { name: 'age', arg: '--version' }
    - { name: 'sops', arg: '--version' }
    - { name: 'skaffold', arg: 'version' }
    - { name: 'helm', arg: 'version --short' } # Added --short for cleaner Helm output
  register: version_outputs
  changed_when: false
  tags: [verify]

- name: Display installed versions
  ansible.builtin.debug:
    msg: "{{ item.item.name | capitalize }}: {{ item.stdout }}"
  loop: "{{ version_outputs.results }}"
  tags: [verify]