---
- name: Ensure apt cache is updated
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: 3600 # Don't update if updated in the last hour

- name: Install base system utilities
  ansible.builtin.apt:
    name:
      - curl
      - wget
      - apt-transport-https
      - jq
      - tar
    state: present

- name: Install Age (Encryption Tool)
  ansible.builtin.unarchive:
    src: "https://github.com/FiloSottile/age/releases/download/{{ age_version }}/age-{{ age_version }}-linux-amd64.tar.gz"
    dest: /usr/local/bin/
    remote_src: yes
    # Extract only the binaries, stripping the parent directory in the tarball
    extra_opts:
      - --strip-components=1
      - age/age
      - age/age-keygen
    creates: /usr/local/bin/age

- name: Install SOPS (Secrets OPerationS)
  ansible.builtin.get_url:
    url: "https://github.com/getsops/sops/releases/download/{{ sops_version }}/sops-{{ sops_version }}.linux.amd64"
    dest: /usr/local/bin/sops
    mode: '0755'

- name: Install Skaffold
  ansible.builtin.get_url:
    url: "https://storage.googleapis.com/skaffold/releases/latest/skaffold-linux-amd64"
    dest: /usr/local/bin/skaffold
    mode: '0755'

- name: Install Helm
  ansible.builtin.unarchive:
    src: "https://get.helm.sh/helm-{{ helm_version }}-linux-amd64.tar.gz"
    dest: /usr/local/bin/
    remote_src: yes
    extra_opts:
      - --strip-components=1
      - linux-amd64/helm
    creates: /usr/local/bin/helm

- name: Check for missing IPv4 Default Gateway (FABRIC Routing Bug)
  ansible.builtin.shell: ip -4 route | grep default || echo "missing"
  register: route_check
  changed_when: false
  become: yes

- name: Identify Management Interface for DHCP (Ignore internal LAN)
  ansible.builtin.set_fact:
    mgmt_iface: "{{ item }}"
  loop: "{{ ansible_interfaces }}"
  when: 
    - item != 'lo'
    - vars['ansible_' + item | replace('-', '_')].ipv4 is defined
    - vars['ansible_' + item | replace('-', '_')].ipv4.address != internal_ip
    - not vars['ansible_' + item | replace('-', '_')].ipv4.address.startswith('10.42')

- name: Restore IPv4 Default Gateway via DHCP
  ansible.builtin.command: dhclient -4 "{{ mgmt_iface }}"
  when: ansible_default_ipv4.gateway is not defined
  become: yes

- name: Clamp MSS to PMTU for IPv4 (FABRIC MTU Fix)
  ansible.builtin.iptables:
    table: mangle
    chain: POSTROUTING
    protocol: tcp
    tcp_flags:
      flags: SYN,RST
      flags_set: SYN
    jump: TCPMSS
    set_mss: 1360
  become: yes

- name: Clamp MSS to PMTU for IPv6 (FABRIC MTU Fix)
  ansible.builtin.iptables:
    table: mangle
    chain: POSTROUTING
    protocol: tcp
    tcp_flags:
      flags: SYN,RST
      flags_set: SYN
    jump: TCPMSS
    set_mss: 1360
    ip_version: ipv6
  become: yes