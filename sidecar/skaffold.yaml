apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: valheim-sidecar

build:
  artifacts:
    - image: valheim-sidecar-image
      context: .

profiles:
  - name: dev
    activation:
      - command: dev
    build:
      local:
        push: false