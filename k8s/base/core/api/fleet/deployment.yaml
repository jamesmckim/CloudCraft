apiVersion: apps/v1
kind: Deployment
metadata:
  name: fleet-service
  labels:
    app: fleet-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fleet-service
  template:
    metadata:
      labels:
        app: fleet-service
    spec:
      serviceAccountName: fleet-service-sa
      containers:
        - name: fleet-service
          image: fleet-api-image
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
              name: http
          env:
            - name: ENVIRONMENT
              value: "development"
      initContainers:
        - name: wait-for-db
          image: postgres:18-alpine
          command:
            - sh
            - -c
            - |
              echo "Waiting for database..."
              until pg_isready -h postgres-service -U user; do
                sleep 2
              done
              echo "Database is ready!"