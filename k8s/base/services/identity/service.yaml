apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: craftcloud-system
spec:
  selector:
    app: identity-service
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http