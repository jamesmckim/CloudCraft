apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: craftcloud-system

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: dashboard-nginx-conf
    files:
      - default.conf